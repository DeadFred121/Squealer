<%= form_with(model: squeal, local: true) do |form| %>
  <% if squeal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(squeal.errors.count, "error") %> prohibited this squeal from being saved:</h2>

      <ul>
      <% squeal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :squeal_content %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
